name: Build Simplipal

on:
  push:
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Verify CMake
        run: cmake --version
