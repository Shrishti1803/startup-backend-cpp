name: Build Simplipal

on:
  push:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build

      - name: Build Project
        run: cmake --build build --config Release
